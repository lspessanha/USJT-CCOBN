<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.siqueira.servicedeskccp3anbua">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_sentiment_very_dissatisfied_black_24dp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_sentiment_very_dissatisfied_black_24dp"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaChamadosActivity" />
        <activity android:name=".DetalhesActivity"></activity>
    </application>

</manifest>